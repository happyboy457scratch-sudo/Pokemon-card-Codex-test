# PokéPrice Scout

A modern single-page app for Pokémon card lookup with:
- Login + sign up system (client-side local auth)
- Search button and instant card lookup
- Card image + market price data fetch

## Run
```bash
python -m http.server 8000
```
Then open `http://localhost:8000`.
